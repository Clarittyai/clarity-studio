{
  "id": "outbound-webhook",
  "kind": "integration",
  "category": "dev",
  "transport": "native",
  "version": "1.0.0",
  "name": "Outbound webhook",
  "vendor": "Claritty",
  "description": "POST results to any webhook URL you own — Zapier, Make, n8n, or your platform's own automations.",
  "intentHints": [
    "notify my system",
    "send results to my platform",
    "push to zapier",
    "send to make.com",
    "trigger my n8n workflow",
    "post to a webhook",
    "forward results to my automation"
  ],
  "authType": "apiKey",
  "providedTools": [
    {
      "id": "outbound-webhook.send",
      "description": "POST a JSON payload to your configured webhook URL — plug this team into Zapier, Make, n8n, or your platform's own automations.",
      "input": {
        "payload": {
          "type": "object",
          "required": true
        },
        "event": {
          "type": "string",
          "required": false
        }
      },
      "output": {
        "ok": {
          "type": "boolean",
          "required": true
        }
      },
      "local": false
    },
    {
      "id": "outbound-webhook.post",
      "description": "POST a JSON payload to a URL.",
      "local": true
    }
  ],
  "credentialFields": [
    {
      "key": "url",
      "label": "Webhook URL",
      "type": "text",
      "required": true,
      "placeholder": "https://hooks.zapier.com/hooks/catch/...",
      "help": "The public HTTPS endpoint to POST to (Zapier/Make/n8n catch hook or your own API)."
    },
    {
      "key": "secret",
      "label": "Signing secret (optional)",
      "type": "password",
      "required": false,
      "help": "Optional shared secret so your receiver can verify calls came from Claritty."
    }
  ],
  "localConnector": true,
  "_localNote": "Studio can broker this. Only tools marked \"local\": true can be called — the rest are hosted-platform only."
}
